# 규모 확장성 부품 8개 — 무엇을 얻고 무엇을 내주는가

## 이 문서의 관점

시스템 설계를 배울 때 대부분의 자료는 부품이 무엇을 해주는지를 설명한다. 캐시는 빠르게 해주고, 로드밸런서는 여러 대로 나눠주고, 큐는 부하를 흡수해준다. 그런데 실무에서 사고가 나는 지점은 거기가 아니다. **부품은 문제를 없애는 게 아니라 다른 문제로 바꾼다.** 캐시를 넣으면 속도 문제가 정합성 문제로 바뀌고, 큐를 넣으면 부하 문제가 중복과 순서 문제로 바뀐다.

그래서 이 문서는 부품마다 다섯 가지를 적는다. 언제 꺼내는가, 무엇을 얻는가, **무엇을 내주는가**, 반드시 따라오는 질문은 무엇인가, 실제로 어디에 쓰이는가.

세 번째 칸이 핵심이다. 그리고 그 칸을 적을 때 "느려진다" 같은 말은 쓰지 않는다. **무엇을 못 하게 되는가, 무엇을 새로 관리해야 하는가**로 적는다. 이 두 가지가 실제로 사람이 감당해야 하는 비용이기 때문이다.

## 부품을 꺼내는 순서

순서 자체보다 각 단계로 넘어가는 이유가 중요하다.

단일 서버에서 출발한다. 웹과 DB를 분리한다. 둘은 늘려야 하는 속도가 다르기 때문이다. 그 다음 로드밸런서를 두고 웹 서버를 여러 대로 늘린다. 웹은 상태가 없어서 늘리기가 가장 쉽다. 그 다음 DB 읽기 사본을 붙인다. 대개 읽기가 쓰기보다 훨씬 많기 때문이다. 그 다음 캐시로 반복 조회를 DB까지 안 보내고, CDN으로 정적 파일을 원본 서버에서 뗀다. 그 다음 무상태화하고 여러 지역에 배치해 지역 장애를 격리한다. 그 다음 메시지 큐로 즉시 응답이 필요 없는 일을 뒤로 뺀다. 샤딩은 가장 마지막이다. 위를 다 해도 쓰기가 안 될 때만 간다.

여기서 자주 오해하는 게 하나 있다. **부품을 많이 넣는 것은 실력이 아니다.** 넣을 때마다 어떤 숫자 때문에 넣는지가 같이 나와야 한다. 필요 없는 부품은 비용과 장애 지점만 늘린다.

---

## 1. 로드밸런서

웹 서버 한 대로 트래픽을 못 받거나, 한 대가 죽으면 서비스가 같이 죽을 때 꺼낸다. 서버를 여러 대로 늘릴 수 있고, 한 대가 죽으면 나머지로 넘어가고, 배포 중에도 트래픽을 빼고 넣을 수 있게 된다.

**대신 서버 안에 상태를 둘 수 없게 된다.** 세션이나 업로드 파일이나 로컬 캐시를 서버에 두면 다음 요청이 다른 서버로 가서 깨진다. 이게 로드밸런서를 넣고 나서 가장 먼저 터지는 문제다. 그리고 로드밸런서 자체가 새로운 단일 장애점이 되기 때문에 이중화해야 한다. "죽었다"를 판정할 기준도 새로 만들어 관리해야 한다. 헬스 체크를 어느 경로로, 몇 초마다, 몇 번 실패하면 뺄 것인지를 정해야 한다. 클라이언트 IP가 로드밸런서 IP로 보이기 때문에 기존 로그와 차단 로직도 틀어진다.

따라오는 질문들.

서버를 늘렸더니 "로그인이 자꾸 풀린다"는 신고가 온다면, 세션이 서버 메모리에 있기 때문이다. 세션을 밖으로 빼거나 토큰 방식으로 바꾼다. 같은 사용자를 항상 같은 서버로 보내는 방식도 있지만, 그 서버가 죽으면 같이 죽으니 임시 처방이다.

로드밸런서가 죽으면 어떻게 하느냐는 질문에는 두 대 이상 두고 상위에서 전환한다고 답한다. 관리형 로드밸런서를 쓰면 그쪽이 이미 다중화되어 있다.

새로 띄운 서버에 트래픽을 보내기 전에 확인할 것은 헬스 체크가 DB와 캐시 연결까지 보는지다. 프로세스만 떠 있고 DB에는 못 붙은 서버로 트래픽이 흘러가는 게 아주 흔한 사고다.

실제로는 Nginx나 HAProxy 같은 소프트웨어 로드밸런서, 또는 클라우드 관리형 로드밸런서를 쓴다. 무중단 배포도 같은 부품을 쓴다. 대상 목록에서 서버를 빼고, 배포하고, 헬스 체크를 통과하면 다시 넣는다. 이걸 전체가 아니라 일부에만 먼저 하는 것이 카나리 배포다.

---

## 2. DB 읽기 사본

읽기가 쓰기보다 훨씬 많고 DB 한 대가 버거울 때, 또는 무거운 조회를 서비스에서 떼고 싶을 때 쓴다. 읽기를 여러 대로 나눌 수 있고, 주 DB가 죽으면 사본을 주로 승격시킬 수 있다.

**대신 쓴 직후 읽으면 데이터가 없을 수 있다.** 복제가 밀리는 동안 사본에는 아직 안 들어와 있기 때문이다. 사용자 입장에서는 "저장했는데 목록에 안 보인다"로 나타난다. 이걸 피하려면 "이 조회는 주 DB로, 이 조회는 사본으로"라는 규칙이 코드에 들어오고, 그 규칙을 아는 사람이 팀에 있어야 유지된다. 더 중요한 건 **쓰기 한계는 하나도 안 풀린다**는 점이다. 문제가 쓰기라면 이 부품은 답이 아니다. 그리고 승격 절차와 판정 기준을 만들어 관리해야 하고, 사본 수만큼 비용이 늘어난다.

따라오는 질문들.

방금 저장한 데이터가 조회에서 안 보이는 이유는 복제 지연이다. 해결은 세 가지 중에 고른다. 쓰기 직후 일정 시간은 주에서 읽거나, 그 화면만 주로 보내거나, 화면에서 방금 쓴 값을 그대로 쓴다.

주 DB가 죽으면 승격에 수십 초에서 분 단위가 걸린다. 그 동안 쓰기는 실패한다. 그래서 실패를 사용자에게 어떻게 보여줄지, 자동으로 재시도할지를 미리 정해둬야 한다.

사본이 밀리는 걸 아는 방법은 복제 지연 초를 지표로 보는 것이다. 임계를 넘으면 그 사본을 읽기 대상에서 뺀다.

실제로는 관리자 통계 화면, 정산과 집계 배치, 검색 조회를 사본으로 보내 서비스 트래픽과 분리한다. 그리고 사용자가 자기가 방금 쓴 글을 보는 화면처럼 복제 지연이 눈에 드러나는 지점만 골라서 주 DB로 예외를 둔다.

---

## 3. 캐시

같은 데이터를 반복해서 읽고, 자주 바뀌지 않고, 조금 옛 값이어도 되는 데이터에 쓴다. DB 부하가 줄고 응답이 빨라진다. DB를 늘리는 것보다 훨씬 싸다.

**대신 원본과 어긋난 값을 보여주는 구간이 생긴다.** 여기서 중요한 건 얼마나 어긋나도 되는지를 개발자가 혼자 정할 수 없다는 점이다. 제품이 정해줘야 한다. 잔액이나 재고처럼 아예 캐시하면 안 되는 데이터도 있다. 그리고 만료와 무효화 규칙이 코드 밖의 관리 대상이 된다. 데이터를 고치는 모든 경로에서 캐시를 지워야 하는데, 경로 하나를 빼먹는 것이 가장 흔한 버그다.

**캐시가 죽으면 그 부하가 그대로 DB로 몰린다.** 여기에 중요한 함의가 있다. 캐시 없이 못 버티는 구조가 되었다면 캐시는 이미 선택 부품이 아니라 필수 부품이고, 필수 부품은 다중화해야 한다. 또 같은 키가 동시에 만료되면 요청이 한꺼번에 DB로 몰린다. 메모리 비용이 들고, "왜 옛 값이 보이나"를 조사할 층이 하나 늘어난다.

따라오는 질문들.

만료를 얼마로 잡느냐는 질문의 답은 "이 데이터가 몇 초 틀려도 되는가"에서 나온다. 짧게 잡으면 DB로 자주 가고 길게 잡으면 오래 틀린다는 맞바꿈으로 설명한다.

캐시 서버가 죽는 순간 전부 DB로 간다. DB가 그 순간의 부하를 못 받으면 연쇄로 죽는다. 그래서 캐시 다중화와 DB 앞 동시 요청 제한을 같이 이야기한다.

데이터를 수정할 때 캐시는 지우는 쪽이 단순하고 안전하다. 캐시에도 같이 쓰면 빠르지만, DB와 캐시 중 하나만 성공하는 경우를 다뤄야 한다.

없는 키를 계속 조회하면 매번 DB로 간다. 그래서 "없음"도 짧게 캐시한다.

실제로는 레디스나 멤캐시드로 상품 상세, 설정값, 인기 목록, 권한 정보처럼 읽기가 압도적인 데이터를 담는다. 주목할 점은 같은 화면 안에서도 데이터마다 판단이 달라진다는 것이다. 상품 이름과 설명은 캐시하고, 재고와 가격은 캐시하지 않거나 아주 짧게 둔다.

---

## 4. CDN

이미지, JS, CSS, 영상처럼 잘 안 바뀌고 누구에게나 같은 파일을, 멀리 있는 사용자가 받아갈 때 쓴다. 사용자 가까운 곳에서 응답하고 원본 서버 트래픽과 비용이 줄어든다.

**대신 파일을 고쳐도 즉시 안 바뀐다.** 무효화를 걸거나 파일명에 버전을 넣는 규칙이 필요한데, 실무에서는 후자가 더 안전하다. 그리고 **사용자마다 다른 내용은 CDN에 올릴 수 없다.** 잘못 캐시되면 남의 정보가 다른 사람에게 보인다. 이 부품에서 터질 수 있는 사고 중 가장 큰 종류다. 전송량 과금이라 트래픽이 늘면 비용도 같이 늘고, "내 화면만 옛 파일이 보인다"를 조사할 때 브라우저인지 CDN인지 원본인지 구분해야 하는 층이 늘어난다.

따라오는 질문들.

파일을 고쳤는데 옛 파일이 계속 보이면, 배포마다 파일명에 해시를 붙여 아예 다른 파일로 만든다. 긴급하면 무효화를 건다.

로그인한 사용자별 화면은 CDN에 둘 수 없다. 캐시 키에 사용자가 안 들어가서 남의 응답이 나갈 수 있기 때문이다. 사용자별 응답은 캐시 금지로 명시한다.

CDN이 죽으면 원본을 직접 가리키는 경로를 남겨둔다. 다만 그때 원본이 그 트래픽을 받을 수 있는지가 진짜 질문이다.

실제로는 이커머스 상품 이미지, 프런트엔드 번들, 폰트, 영상 조각에 쓴다. 빌드할 때 파일명에 해시를 넣고 캐시 기간을 아주 길게 잡는 방식이 사실상 표준이다. 파일이 바뀌면 이름이 바뀌므로 무효화 자체가 필요 없어진다.

---

## 5. 무상태 웹 계층

서버를 자유롭게 늘리고 죽이고 갈아치우고 싶을 때 필요하다. 사실 로드밸런서를 넣는 순간 거의 강제된다. 어느 서버가 받아도 결과가 같아지고, 증설과 교체와 재배포가 쉬워지고, 서버 한 대를 언제든 버릴 수 있게 된다.

**대신 상태를 옮겨놓을 저장소가 새로 필요해지고, 그게 새 의존성이 된다.** 여기서 중요한 인식이 하나 있다. 세션 저장소가 죽으면 전원 로그아웃된다. 즉 상태를 없앤 게 아니라 **장애점을 옮긴 것**이다. 그리고 매 요청마다 그 저장소를 읽으니 왕복이 늘어나고, 서버 로컬 캐시로 벌던 이득을 포기하게 된다. 업로드 파일도 서버 디스크에 못 두기 때문에 파일 저장소가 따라 들어온다.

따라오는 질문들.

로그인 상태는 공용 저장소에 두거나, 서명된 토큰을 써서 서버가 아무것도 안 들고 있게 한다. 토큰 방식은 즉시 무효화가 어렵다는 대가가 있다.

그 저장소가 죽으면 실제로 전원 로그아웃이 맞다. 그래서 다중화한다. 토큰 방식이면 이 장애가 아예 없는 대신 강제 로그아웃을 포기하는 것이다.

업로드 파일은 오브젝트 스토리지로 보낸다. 서버 디스크에 두면 그 서버로만 조회가 되고, 결국 그 서버를 버릴 수 없게 된다.

실제로는 세션을 레디스에 모으거나 서명 토큰을 발급한다. 업로드는 오브젝트 스토리지로 보내고 서버는 주소만 저장한다. 컨테이너로 배포하는 환경에서는 서버가 수시로 죽고 새로 뜨기 때문에 이 조건이 선택이 아니라 전제가 된다.

---

## 6. 메시지 큐

받는 쪽과 처리하는 쪽을 떼어놔도 될 때, 순간 몰리는 양을 흡수해야 할 때, 오래 걸리는 일을 응답에서 빼야 할 때 쓴다. 보내는 쪽과 처리하는 쪽을 따로 늘릴 수 있고, 처리 쪽이 잠깐 죽어도 요청이 안 버려지고, 실패한 건을 다시 처리할 수 있다.

**대신 결과가 즉시 안 나온다.** "처리 중" 상태와 그걸 보여줄 화면을 만들어야 하므로 서버만의 일이 아니게 된다. 그리고 **최소 한 번 배달이 기본이라 중복이 온다.** 받는 쪽을 멱등하게 만들어야 한다. 안 하면 알림이 두 번 가고 금액이 두 번 적립된다. **순서 보장도 기본이 아니다.** 순서가 필요하면 같은 키를 같은 곳으로 묶어야 하고, 그러면 그 키에만 몰릴 수 있다. 적체를 볼 지표와 계속 실패하는 건을 따로 모을 곳이 필요하다. 없으면 한 건이 큐 전체를 막는다. 그리고 장애 추적이 어려워진다. 요청과 처리가 다른 시각, 다른 로그에 남기 때문이다.

따라오는 질문들.

같은 메시지가 두 번 오면, 받는 쪽에서 처리 키를 유니크 제약으로 선점하고 이미 있으면 조용히 끝낸다. 두 번 실행돼도 결과가 같게 만드는 것이 목표다.

순서가 중요한 작업이면 같은 대상의 메시지를 같은 파티션으로 보낸다. 전체 순서는 포기한다.

쌓이는 속도가 처리 속도보다 빠른 걸 알아채려면 큐 길이만 보면 늦다. 가장 오래된 메시지의 나이를 같이 봐야 한다.

사용자에게는 접수 응답을 먼저 주고, 상태를 조회하게 하거나 완료를 밀어준다.

실제로는 주문 뒤에 붙는 알림과 집계와 정산, 대량 문자와 푸시 발송, 이미지나 영상 변환처럼 오래 걸리는 작업에 쓴다. 외부 서비스 호출도 큐로 뺀다. 상대가 느리거나 죽었을 때 내 응답이 같이 느려지지 않게 하려는 것이다. 대신 재시도와 중복 처리를 내가 책임져야 한다.

---

## 7. 샤딩

쓰기가 DB 한 대의 한계를 넘을 때, 또는 데이터가 한 대에 안 들어갈 때 쓴다. 앞의 부품을 다 쓴 다음이다. 쓰기 처리량과 저장 용량을 대수만큼 늘릴 수 있고, 한 샤드 장애가 전체 장애가 아니게 된다.

**대신 샤드를 넘는 조인과 집계를 포기한다.** 전체 통계는 따로 모으는 경로를 만들어야 한다. **트랜잭션도 한 샤드 안으로 제한해야 한다.** 두 샤드에 걸친 원자적 처리는 안 되기 때문에, 설계 단계에서 같이 바뀔 것끼리 같은 샤드에 들어가도록 짜야 한다.

이 부품에는 다른 부품에 없는 특성이 하나 있다. **키를 잘못 잡으면 되돌리기가 매우 비싸다.** 다른 부품은 대체로 나중에 고치면 되지만 여기서는 그게 안 통한다. 샤드 수를 바꾸는 것 자체가 프로젝트다. 서비스를 세우지 않고 재분배해야 하기 때문이다. 그리고 특정 샤드만 몰리는 것을 계속 감시해야 한다. 인기 있는 대상 하나가 한 샤드를 태울 수 있다.

따라오는 질문들.

샤딩 키는 가장 많이 조회하는 단위로 잡는다. 그러면 대부분의 조회가 한 샤드 안에서 끝난다. 사용자 화면이 많은 서비스면 사용자 기준이 된다.

특정 샤드에만 몰리면 그 키를 쪼개서 분산하거나 그 대상만 따로 뺀다. 다만 먼저 샤드별 지표가 있어야 알 수 있다.

샤드를 8개에서 16개로 늘리는 경우, 나머지 연산으로 나눴다면 거의 전체를 옮겨야 한다. 키를 원형 구간에 배치하는 방식을 쓰면 옮기는 양이 크게 준다.

여러 샤드에 걸친 조회나 집계는 각 샤드에 물어서 합치거나, 조회 전용 저장소에 미리 모아둔다.

실제로는 메신저 대화 기록, 대량 로그, 이력 테이블처럼 쓰기가 계속 쌓이고 대상별로 조회가 갈리는 데이터에 쓴다. 안 해본 사람이 할 수 있는 최선은 **순서를 아는 것**이다. 먼저 읽기 사본과 캐시로 읽기를 덜고, 오래된 데이터를 떼내고, 그래도 쓰기가 남을 때 샤딩으로 간다. 그리고 키는 조회 단위와 맞춘다.

---

## 8. 여러 지역 배치

특정 지역 사용자의 지연이 크거나, 한 지역이 죽어도 서비스를 유지해야 할 때 쓴다. 가까운 곳으로 보낼 수 있고 한 지역 장애를 격리할 수 있다.

**대신 데이터를 맞추는 문제가 새로 들어온다.** 지역 간 지연 때문에 즉시 일치는 대개 포기한다. 양쪽에 같은 대상의 쓰기가 들어오면 어느 쪽이 이기는지 규칙을 정해야 한다. 배포와 설정과 스키마 변경을 여러 곳에 맞춰야 하는데, **한쪽만 배포된 상태가 새로운 장애 유형**이 된다. 비용과 테스트 대상이 배로 늘어나고, 무엇보다 한 지역을 실제로 빼보지 않으면 이게 되는지 알 수 없다.

따라오는 질문들.

양쪽 데이터를 맞추는 가장 단순한 방법은 한쪽만 쓰기를 받고 다른 쪽은 읽기로 두는 것이다. 양쪽 쓰기는 충돌 규칙까지 필요해서 마지막에 고른다.

두 지역 사이가 끊긴 동안 양쪽에 쓰기가 들어오면, 둘 다 받으면 나중에 충돌하고 한쪽을 막으면 그 지역이 멈춘다. 데이터 성격으로 고른다. 돈은 막고, 조회 기록은 받는다.

한 지역을 빼는 훈련을 해봤느냐는 질문에는 안 해봤으면 안 해봤다고 답한다. 대신 정기적으로 한쪽을 빼보는 절차를 넣겠다는 답까지 준비한다.

실제로는 글로벌 서비스가 지역별로 같은 구성을 두고 가까운 곳으로 보낸다. 처음에는 한 지역만 쓰기를 받는 형태로 시작하는 게 일반적이다. 같은 판단을 작게 하면 카나리 배포가 된다. **전체를 한 번에 바꾸지 않고 일부에서 먼저 확인한다**는 것이 두 부품의 공통 원리다.

---

## 정리 — 부품별 대가 한 줄 요약

| 부품 | 무엇을 내주는가 |
|---|---|
| 로드밸런서 | 서버에 상태를 둘 수 없게 된다 |
| DB 읽기 사본 | 쓴 직후 읽으면 없을 수 있다. 쓰기 한계는 안 풀린다 |
| 캐시 | 틀린 값을 보여주는 구간이 생긴다. 캐시가 죽으면 부하가 DB로 몰린다 |
| CDN | 고쳐도 즉시 안 바뀐다. 사용자별 내용은 못 올린다 |
| 무상태 웹 계층 | 장애점을 없앤 게 아니라 저장소로 옮긴 것이다 |
| 메시지 큐 | 즉시 응답을 포기한다. 중복과 순서를 내가 책임진다 |
| 샤딩 | 조인·집계·교차 트랜잭션을 포기한다. 키를 잘못 잡으면 되돌리기가 매우 비싸다 |
| 여러 지역 배치 | 즉시 일치를 포기한다. 한쪽만 배포된 상태가 새 장애가 된다 |

## 익혔다고 할 수 있는 기준

책을 덮고 종이에 단일 서버부터 부품이 붙는 순서를 그린다. 그림보다 각 화살표에 "왜 여기서 이걸 넣나"가 한 줄씩 붙는 게 중요하다. 각 부품의 따라오는 질문에 답이 나오는지 확인하고, 안 나오는 것을 다음에 볼 목록으로 삼는다. 부품을 넣는 이유뿐 아니라 **안 넣는 이유**도 한 줄씩 준비한다. 다 넣는 사람이 아니라 필요한 것만 넣는 사람이 되는 것이 목표다.

그리고 최종 기준은 이것이다. **minus를 안 보고 말할 수 있으면 그 부품은 익힌 것이다.** plus는 검색하면 나오지만 minus는 잘 안 나온다.

---

『가상 면접 사례로 배우는 대규모 시스템 설계 기초』 1장을 읽으며 정리한 개인 학습 노트. 내용은 본인 표현으로 다시 쓴 것이고, 오류가 있으면 제 책임입니다.
